/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Для старых версий Next.js используйте pages/ вместо app/
  // Раскомментируйте если используете Pages Router:
  // pageExtensions: ['tsx', 'ts', 'jsx', 'js'],
}

module.exports = nextConfig

